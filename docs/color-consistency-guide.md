# 配色一致性规范

## 概述

本文档定义了 TrionDesign 项目中所有组件的配色规范，确保同类组件使用一致的配色方案。

---

## 🎨 核心配色规范

### 1. 状态徽章（Badge）

状态徽章用于显示关键状态信息，如告警级别、网络关联状态等。

#### 成功/正常状态
```tsx
className="bg-green-600 dark:bg-green-600 text-white"
```
- **用途**：表示正常、健康、无问题的状态
- **示例**：
  - `NetworkCorrelationSidebar` - "Not Correlated" 徽章
  - `NetworkAssessment` - "No Network Impact" 徽章

#### 警告/问题状态
```tsx
className="bg-amber-300 dark:bg-amber-300 text-neutral-900 dark:text-neutral-900"
```
- **用途**：表示警告、需要关注的问题
- **示例**：
  - `NetworkCorrelationSidebar` - "Correlated" 徽章
  - `NetworkAssessment` - "Network Impact Detected" 徽章
  - `BusinessImpactSection` - "Most Impacted" 项目徽章

#### 严重/错误状态
```tsx
className="bg-red-600 dark:bg-red-600 text-white"
```
- **用途**：表示严重告警、错误状态
- **示例**：
  - `AlertSummaryChart` - "CRITICAL" 徽章

---

### 2. 信息背景块（Info Block）

信息背景块用于展示状态说明、评估结果等信息。

#### 成功/正常状态背景
```tsx
className="bg-green-50/60 dark:bg-green-900/25 border-l-4 border-green-500 dark:border-green-400"
```
- **用途**：表示正常、健康的状态说明
- **示例**：
  - `NetworkAssessment` - 健康状态背景
  - `NetworkCorrelationSidebar` - 未关联状态背景（折叠）
  - `NetworkCorrelationSidebar` - 正常状态解释背景（展开）

#### 警告/问题状态背景
```tsx
className="bg-amber-50/60 dark:bg-amber-800/35 border-l-4 border-amber-500 dark:border-amber-400"
```
- **用途**：表示警告、需要关注的问题说明
- **示例**：
  - `NetworkAssessment` - 警告状态背景
  - `NetworkCorrelationSidebar` - 关联状态背景（折叠）
  - `NetworkCorrelationSidebar` - 警告状态解释背景（展开）

---

### 3. 状态指示点（Status Indicator Dot）

状态指示点用于在按钮或标签中快速显示状态。

#### 成功/正常状态
```tsx
className="w-1.5 h-1.5 rounded-full bg-green-500"
```

#### 警告/问题状态
```tsx
className="w-1.5 h-1.5 rounded-full bg-amber-400"
```

- **示例**：
  - `NetworkCorrelationSidebar` - Availability/Performance 标签页指示点

---

### 4. SectionHeader 图标容器

用于区块标题的图标容器，支持多种颜色变体。

#### 红色（错误/告警）
```tsx
container: 'bg-red-100 dark:bg-red-900/50'
icon: 'text-red-600 dark:text-red-300'
```

#### 蓝色（信息）
```tsx
container: 'bg-blue-100 dark:bg-blue-900/50'
icon: 'text-blue-600 dark:text-blue-300'
```

#### 绿色（成功）
```tsx
container: 'bg-green-100 dark:bg-green-900/50'
icon: 'text-green-600 dark:text-green-300'
```

#### 中性（默认）
```tsx
container: 'bg-neutral-100 dark:bg-neutral-700'
icon: ''
```

- **定义位置**：`src/components/index.tsx` - `SectionHeader` 组件
- **符合规范**：与 `docs/design-token.md` 第 99-102 行的强调色规范一致

---

### 5. 表格行高亮（Table Row Highlight）

用于突出显示离群值或重要数据行。

```tsx
className="bg-amber-300 dark:bg-amber-300 text-neutral-900 dark:text-neutral-900"
```

- **用途**：表格中的离群值高亮
- **定义位置**：`src/utils/tableColoring.ts`
- **示例**：Business Impact 各维度表格的离群值行

---

## 🔍 配色一致性检查清单

在添加或修改组件时，请确保：

- [ ] **徽章配色**：成功用绿色 `green-600`，警告用琥珀色 `amber-300`，错误用红色 `red-600`
- [ ] **深浅模式一致**：所有配色都必须提供 `dark:` 变体
- [ ] **背景块配色**：成功用绿色 `green-50/60 + green-900/25`，警告用琥珀色 `amber-50/60 + amber-800/35`
- [ ] **状态指示点**：成功用 `green-500`，警告用 `amber-400`
- [ ] **文字对比度**：确保文字在背景上有足够的对比度（琥珀色背景用深色文字，绿色/红色背景用白色文字）

---

## 📋 已修复的配色不一致问题

### 2025-11-05 修复记录

1. **NetworkCorrelationSidebar.tsx:203** - 展开状态的 Correlated 徽章
   - 修复前：`bg-amber-300 text-amber-900 dark:bg-amber-500 dark:text-amber-950`
   - 修复后：`bg-amber-300 dark:bg-amber-300 text-neutral-900 dark:text-neutral-900`

2. **NetworkCorrelationSidebar.tsx:202** - 展开状态的 Not Correlated 徽章
   - 修复前：`bg-green-600 text-white`（缺少 dark 变体）
   - 修复后：`bg-green-600 dark:bg-green-600 text-white`

3. **NetworkCorrelationSidebar.tsx:432** - 正常状态解释背景
   - 修复前：`bg-blue-50/60 dark:bg-blue-900/25`（使用蓝色）
   - 修复后：`bg-green-50/60 dark:bg-green-900/25`（统一为绿色）

---

## 🎯 设计原则

1. **语义化配色**：颜色应传达明确的语义（绿色=成功，琥珀色=警告，红色=错误）
2. **深浅模式对称**：深色模式和浅色模式应使用相同的语义配色
3. **对比度优先**：确保文字在任何背景上都清晰可读
4. **一致性优先**：同类组件必须使用完全一致的配色方案
5. **可维护性**：配色应集中定义，便于统一修改

---

## 📚 参考文档

- [设计规范文档](./design-token.md) - 完整的设计系统规范
- [表格着色逻辑](./table-coloring-logic.md) - 表格离群值着色算法

---

**最后更新时间**：2025-11-05

